ТИПЫ КОМАНД:
  * ИНИЦИАЛИЗАЦИЯ ДРАЙВЕРА:
        создание объекта подходящего класса - вызывается конструктор который создает объект driver = new ChromeDriver();
        при вызове конструктора запускается вспомогательный файл который запускает браузер
    НАСТРОЙКА ДРАЙВЕРА:
        после того как драйвер создан иногда необходимо в нем настроить дополнительные параметры
        например установка различных timout, например ожидание загрузки страницы
        также иногда необходимо настроить параметры браузера, например добавление/удаление coockies
  * ДЕЙСТВИЯ СО СТРАНИЦАМИ
        загрузка страниц
        обновление страницы (refresh)
        переход на следующую/предыдущую страницу (forward/back)
        здесь есть несколько скрытых подводных камней и ловушек
    ДЕЙСТВИЯ С ОКНАМИ
        selenium не может открывать окна самостоятельно (только при помощи js)
        умеет переключаться с одного окна на другое
        умеет закрывать текущее окно и если окно последнее - браузер закрывается
        здесь есть несколько скрытых подводных камней и ловушек
    ДЕЙСТВИЯ С ДИАЛОГОВЫМИ ОКНАМИ
        эти окна содержат кнопи ok или cancel
        также иногда туда можно вводить текст
        либо через эти окна можно загружать/скачивать файлы на сервер или с сервера на локальный диск
        selenium не работает с некоторыми диалогами - поэтому их нужно научиться избегать
  * ПОИСК ЭЛЕМЕНТОВ (это пассивные команды)
        selenium  поддерживает несколько разных стратегий поиска элементов
    СВОЙСТВА ЭЛЕМЕНТОВ (это пассивные команды)
        получение свойств выбранного элемента (tag, attributes, text, styles, location)
  * ДЕЙСТВИЯ С ЭЛЕМЕНТАМИ
        selenium  поддерживает простые и сложные (комбинации простых) действия
    ДЕЙСТВИЯ С ФРЕЙМАМИ (окна вложенные внутрь страницы)
        для этого существует отдельный класс команд
    ВЫПОЛНЕНИЕ JS-КОДА
        selenium позволяет выполнить произвольный js код на страницы приложения
        если какое-то действие selenium не может выполнить, его может выполнить js

 * команды присутствующие в любом сценарии с использованием selenium
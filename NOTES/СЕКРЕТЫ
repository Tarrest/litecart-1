
1) элементы могут пропадать со страницы
    поэтму необходимо искать нужный элемент непосредственно перед действием:
    findElement().sendKeys();
    findElement().click();

2) элемент должен быть видимым

3) если элемент не единственны - selenium возвращает первый элемент

4) если элементы отсутствуют:
    findElement выбрасывает исключение NoSuchElementException
    findElements возвращает пустой список

5) StaleElementReferenceException означает что элемент исчез со страницы

6) Capabilities - selenium может передавать или получать настройки
    - выбирать правильный браузер
    - настроить драйвер
    - настроить браузер (опций командной строки гораздо больше чем можно поменять у capabilities)

7) Стратегии поиска (совпадают с js и selenium):
       By.id (связан с CSS)
       By.tagName (связан с CSS)
       By.className (связан с CSS)
       By.cssSelector (связан с CSS)

       By.name (для поиска форм)

       By.xpath (xpath язык запросов поддерживается не всеми браузерами)

       By.linkText (поиск по тексту ссылки)
       By.partialLinkText (поиск по частичному тексту ссылки)
       findElements(By.tagName("a"));
8) FindElements - возвращает список элементотв из DOM по порядку (ищем если просто читаем страницу)
   FindElemenet - возвращает первый элемент из этого списка (ищем если нужно сделать какието дествия с ним)


